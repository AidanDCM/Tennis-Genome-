name: API-Tennis Prospective WTA Shadow Evidence

on:
  workflow_dispatch:
    inputs:
      prediction_artifact_id:
        description: Trusted pre-match Champion prediction artifact ID
        required: true
        type: string
      history_artifact_id:
        description: Retained filtered API-Tennis history artifact ID
        required: true
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: api-tennis-prospective-evidence-${{ inputs.prediction_artifact_id }}
  cancel-in-progress: false

jobs:
  capture:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      PYTHONPATH: src
      GH_TOKEN: ${{ github.token }}
      API_TENNIS_API: ${{ secrets.API_TENNIS_API }}
      PREDICTION_ARTIFACT_ID: ${{ inputs.prediction_artifact_id }}
      HISTORY_ARTIFACT_ID: ${{ inputs.history_artifact_id }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11.16"
          check-latest: false

      - name: Validate trusted inputs
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_EVENT_NAME}" = "workflow_dispatch"
          test "${GITHUB_REF_NAME}" = "main"
          test "${GITHUB_REPOSITORY}" = "AidanDCM/Tennis-Genome-"
          test -n "${API_TENNIS_API:-}"
          for value in "${PREDICTION_ARTIFACT_ID}" "${HISTORY_ARTIFACT_ID}"; do
            case "$value" in
              ''|*[!0-9]*) echo "artifact IDs must be positive integers" >&2; exit 1 ;;
            esac
            test "$value" -gt 0
          done

      - name: Download Champion and retained API-Tennis history artifacts
        shell: bash
        run: |
          set -euo pipefail
          download_artifact() {
            artifact_id="$1"
            zip_path="$2"
            out_dir="$3"
            metadata_path="$4"
            mkdir -p "$out_dir"
            gh api \
              -H "Accept: application/vnd.github+json" \
              "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${artifact_id}" \
              > "$metadata_path"
            gh api \
              -H "Accept: application/vnd.github+json" \
              "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${artifact_id}/zip" \
              > "$zip_path"
            unzip -q "$zip_path" -d "$out_dir"
          }
          download_artifact \
            "$PREDICTION_ARTIFACT_ID" \
            champion-artifact.zip \
            champion-artifact \
            champion-artifact-metadata.json
          download_artifact \
            "$HISTORY_ARTIFACT_ID" \
            history-artifact.zip \
            history-artifact \
            history-artifact-metadata.json

          test -f champion-artifact/prediction-work/prediction-dossier.json
          mapfile -t targets < <(
            find champion-artifact -maxdepth 1 -type f \
              -name 'forward-*-target-resolution.json' | sort
          )
          test "${#targets[@]}" -eq 1
          printf '%s\n' "${targets[0]}" > target-resolution-path.txt

          test -f history-artifact/api-tennis-filtered-range/raw-wta-fixtures.json
          test -f history-artifact/api-tennis-filtered-range/manifest.json
          sha256sum champion-artifact.zip history-artifact.zip > input-artifact-sha256.txt

      - name: Verify retained history contract
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          base = Path('history-artifact/api-tennis-filtered-range')
          manifest = json.loads((base / 'manifest.json').read_text(encoding='utf-8'))
          raw_wta = (base / 'raw-wta-fixtures.json').read_bytes()
          if hashlib.sha256(raw_wta).hexdigest() != manifest['raw_wta_sha256']:
              raise SystemExit('retained WTA raw hash mismatch')
          if manifest.get('historical_actual_start_admissible') is not False:
              raise SystemExit('retained history chronology policy mismatch')
          if manifest.get('provider_request_count') != 2:
              raise SystemExit('retained history is not the frozen filtered capture contract')
          PY

      - name: Install pinned runtime
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -r requirements/ci.lock
          python -m pip install -e . --no-deps

      - name: Spend exactly one bounded WTA fixtures request and build evidence
        shell: bash
        run: |
          set -euo pipefail
          target=$(cat target-resolution-path.txt)
          python scripts/build_api_tennis_prospective_evidence.py \
            --prediction-dossier champion-artifact/prediction-work/prediction-dossier.json \
            --target-resolution "$target" \
            --history-wta history-artifact/api-tennis-filtered-range/raw-wta-fixtures.json \
            --champion-prediction-artifact-id "$PREDICTION_ARTIFACT_ID" \
            --history-artifact-id "$HISTORY_ARTIFACT_ID" \
            --output-dir api-tennis-prospective-evidence \
            | tee api-tennis-prospective-evidence/summary.json

      - name: Verify prospective evidence invariants
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path

          root = Path('api-tennis-prospective-evidence')
          manifest = json.loads((root / 'manifest.json').read_text(encoding='utf-8'))
          state = json.loads(
              (root / 'api-tennis-prospective-state.json').read_text(encoding='utf-8')
          )
          crosswalk = json.loads(
              (root / 'api-tennis-champion-crosswalk.json').read_text(encoding='utf-8')
          )
          if manifest.get('provider_request_count') != 1:
              raise SystemExit('prospective evidence request count is not exactly one')
          if manifest.get('market_blind') is not True:
              raise SystemExit('prospective evidence is not market blind')
          if manifest.get('target_outcome_consumed') is not False:
              raise SystemExit('target outcome leakage flag is not false')
          if manifest.get('same_day_history_consumed') is not False:
              raise SystemExit('same-day history leakage flag is not false')
          if state.get('historical_actual_start_admissible') is not False:
              raise SystemExit('prospective state chronology policy drifted')
          if crosswalk.get('mapping_basis') != 'EXACT_NORMALIZED_NAME_PREMATCH':
              raise SystemExit('crosswalk mapping basis drifted')
          for forbidden in ('winner_side', 'outcome', 'market', 'odds', 'bookmaker'):
              if forbidden in json.dumps(state).lower():
                  raise SystemExit(f'forbidden target field reached state evidence: {forbidden}')
          PY

      - name: Build GitHub provenance receipt
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          import json
          import os
          from datetime import UTC, datetime
          from pathlib import Path

          root = Path('api-tennis-prospective-evidence')
          manifest_bytes = (root / 'manifest.json').read_bytes()
          manifest = json.loads(manifest_bytes)
          champion_meta = json.loads(
              Path('champion-artifact-metadata.json').read_text(encoding='utf-8')
          )
          history_meta = json.loads(
              Path('history-artifact-metadata.json').read_text(encoding='utf-8')
          )
          receipt = {
              'schema_version': (
                  'tennis-genome-api-tennis-prospective-evidence-github-receipt-v1'
              ),
              'repository': os.environ['GITHUB_REPOSITORY'],
              'workflow_run_id': int(os.environ['GITHUB_RUN_ID']),
              'workflow_run_attempt': int(os.environ['GITHUB_RUN_ATTEMPT']),
              'workflow_source_sha': os.environ['GITHUB_SHA'],
              'champion_prediction_artifact_id': int(
                  os.environ['PREDICTION_ARTIFACT_ID']
              ),
              'champion_prediction_artifact_digest': champion_meta.get('digest'),
              'history_artifact_id': int(os.environ['HISTORY_ARTIFACT_ID']),
              'history_artifact_digest': history_meta.get('digest'),
              'provider_request_count': manifest['provider_request_count'],
              'query_date_start': manifest['query_date_start'],
              'query_date_stop': manifest['query_date_stop'],
              'evidence_sha256': manifest['evidence_sha256'],
              'crosswalk_sha256': manifest['crosswalk_sha256'],
              'build_record_sha256': manifest['build_record_sha256'],
              'manifest_file_sha256': hashlib.sha256(manifest_bytes).hexdigest(),
              'created_at_utc': datetime.now(UTC).isoformat(),
          }
          (root / 'github-receipt.json').write_text(
              json.dumps(receipt, indent=2, sort_keys=True) + '\n',
              encoding='utf-8',
          )
          PY

      - name: Upload complete prospective API-Tennis evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: api-tennis-prospective-evidence-${{ inputs.prediction_artifact_id }}
          path: |
            api-tennis-prospective-evidence/api-tennis-prospective-state.json
            api-tennis-prospective-evidence/api-tennis-champion-crosswalk.json
            api-tennis-prospective-evidence/build-record.json
            api-tennis-prospective-evidence/manifest.json
            api-tennis-prospective-evidence/raw-wta-extension.json
            api-tennis-prospective-evidence/summary.json
            api-tennis-prospective-evidence/github-receipt.json
          if-no-files-found: error
          retention-days: 90
